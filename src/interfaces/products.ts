export default interface obj {
    productId: string;
    name: string;
    image: string;
    description: string;
    price: number;
    Category: category;
}

export interface category {
    name:string
}