export default interface obj {
    id: number;
    name: string;
    image: string;
    description: string;
    price: number;
    category: string;
    id?: number;
}

export interface category {
    name:string
}