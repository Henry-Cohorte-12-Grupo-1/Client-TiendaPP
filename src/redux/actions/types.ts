//This are the actions for the reducer's switch-case, add more if necessary
export enum ActionTypes {
    ADD_ONE,
    ACTION_0,
    ACTION_1,
    ACTION_N,
    BRING_PRODUCTS,
    SEARCH_PRODUCT,
    ORDER_BY_CATEGORY,
    GET_CATEGORIES,
    GET_DETAILS,
    GET_USER_PRODUCTS,
    SEARCH_PRODUCT_AC
}
